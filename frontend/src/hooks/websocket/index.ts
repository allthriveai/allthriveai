/**
 * WebSocket Hooks
 *
 * Exports the base WebSocket hook and related types.
 */

export { useWebSocketBase } from './useWebSocketBase';
export type {
  WebSocketBaseOptions,
  WebSocketBaseReturn,
  WebSocketLogger,
} from './types';
export { WS_DEFAULTS } from './types';
