# Backend API URL (used by Vite proxy for /api routes)
# Default: http://localhost:8000 (for local development)
# Docker: http://web:8000 (set in docker-compose.yml)
VITE_API_URL=http://localhost:8000

# WebSocket URL for direct connections to backend
# ARCHITECTURE: We use DIRECT WebSocket connections (not proxied through Vite)
# This provides production parity and easier debugging.
#
# Development: ws://localhost:8000 (default - connects directly to Django Channels)
# Production: wss://api.allthrive.ai (or your production domain)
#
# Leave unset to use the default (ws://localhost:8000)
# VITE_WS_URL=ws://localhost:8000

# Application URL (for SEO, canonical URLs, and meta tags)
# Development: http://localhost:3000
# Production: https://allthrive.ai
VITE_APP_URL=http://localhost:3000

# Stripe Configuration
# Get your publishable key from: https://dashboard.stripe.com/test/apikeys
# Test key starts with: pk_test_
# Production key starts with: pk_live_
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Stripe Payment Configuration
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Sentry Error Tracking (Production only)
# Get your DSN from: https://sentry.io/settings/projects/
# Leave blank to disable error tracking
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_APP_VERSION=1.0.0
# Sentry sample rates (0.0 to 1.0)
# - Traces: Performance monitoring sample rate
# - Replays Session: Session replay sample rate for all sessions
# - Replays Error: Session replay sample rate when errors occur
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1
VITE_SENTRY_REPLAYS_ERROR_SAMPLE_RATE=1.0

# reCAPTCHA v3 Configuration
# Get your site key from: https://www.google.com/recaptcha/admin
# Use the reCAPTCHA v3 type for invisible protection
VITE_RECAPTCHA_SITE_KEY=
